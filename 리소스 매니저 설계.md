# D3D 리소스 관리



## 공유 리소스의 처리

```c++
// 리소스 매니저가 관리할 수 있는 자원의 기본단위
class Resource
{
public:
    Resource() = default;
    virtual ~Resource() = default;
};
```

- 리소스매니저에 의해 관리되는 모든 리소스는 "Resource" 클래스를 상속받아야 합니다.
- 이로서 모든 리소스는 리소스매니저의 Load<T>함수로 일관성있게 처리됩니다.

```c++
std::unordered_map<std::string, std::weak_ptr<Resource>> m_resourceTable;

template <typename T>
std::shared_ptr<T> Load(std::string key)
{
	//키가 있는 지 확인
	auto resource_iter = m_resourceTable.find(key);

	if (resource_iter != m_resourceTable.end())
	{
		auto res_shared = resource_iter->second.lock();
		if (res_shared)
			if (auto derived_shared = std::dynamic_pointer_cast<T>(res_shared))
				return derived_shared;

		m_resourceTable.erase(resource_iter);
	}

	//없는 경우 새로 만들기
	auto newRes = std::make_shared<T>();
	m_resourceTable[key] = newRes;
	return newRes;
}
```



```c++
class Mesh : public Resource
{
private:
    friend class AssimpConverter;

    ComPtr<ID3D11Buffer> m_pVertexBuffer, m_pIndexBuffer;
    std::vector<DefaultVertex> m_vertices;
    std::vector<UINT> m_indices;
    BoundingBox m_bbox;

    inline void SetVertices(std::vector<DefaultVertex>&& vertices) { m_vertices = std::move(vertices); }
    inline void SetIndices(std::vector<UINT>&& indices) { m_indices = std::move(indices); }
    inline void SetBBox(BoundingBox&& bbox) { m_bbox = std::move(bbox); }
public:

    inline std::vector<DefaultVertex>& GetVertices() { return m_vertices; }
    inline std::vector<UINT>& GetIndices() { return m_indices; }
    inline const BoundingBox& GetBBox() const { return m_bbox; }

    void CreateBuffers(ID3D11Device* pDevice);

    Mesh();
    ~Mesh();
    void Bind(ID3D11DeviceContext* ctx);
};
```

- 또한 Resource를 상속받은 객체에 D3D 리소스가 필요한 경우 ComPtr 타입의 멤버로 클래스에 추가하여 해당 Resource가 해제되면 반드시 짝지어진 D3D 리소스도 함께 해제될 수 있도록 강제하였습니다.
- 추가적으로 D3D리소스를 멤버로 소유한 리소스들은 엔진 로직에서 개념적으로 쪼갤 수 없는 단계까지 추상화시켜 중복으로 자원이 생성되는 일이 없도록 강제하였습니다.
